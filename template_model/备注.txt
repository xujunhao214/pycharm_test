allure.attach(
    body,          # 附件的具体内容（字符串/二进制数据/文件路径）
    name,          # 附件在报告中显示的名称（自定义，如“请求参数”“错误截图”）
    attachment_type,  # 附件类型（Allure 预定义，决定展示方式）
    extension=None  # 可选，附件文件扩展名（如 ".json" ".png"，部分类型会自动推导）
)
# 以阿里云镜像为例，安装依赖（替换成你的实际命令，如安装requirements.txt）
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
常用国内镜像源列表：
阿里云：https://mirrors.aliyun.com/pypi/simple/
豆瓣：https://pypi.doubanio.com/simple/
清华：https://pypi.tuna.tsinghua.edu.cn/simple/